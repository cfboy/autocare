module.exports = historyTypes = { SERVICE: 'Service', USER_ACTION: 'User Action', CUSTOMER_ACTION: 'Customer Action' }

// Service Type is to tag all services.
// User Actions Type is to tag all actions by user.